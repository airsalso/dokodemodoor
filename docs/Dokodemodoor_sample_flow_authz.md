# Dokodemodoor Authorization (AUTHZ) Analysis Flow Audit

## 📋 점검 대상 경로 (AUTHZ 중심)
1. **[기초] Pre-Recon**: `pre-recon-code.txt` (RBAC/ABAC 패턴 및 테넌트 격리 구조 포착)
2. **[정찰] Recon**: `recon.txt` (IDOR 후보지 매핑 및 권한 아키텍처 도식화)
3. **[검증] Recon-Verify**: `recon-verify.txt` (코드 레벨에서 권한 가드(Guard)의 위치 및 지배력 확인)
4. **[동적] API Fuzzer**: `api-fuzzer.txt` (권한 없는 사용자로의 요청을 통한 BOLA/IDOR 징후 포착)
5. **[분석] Vuln-AUTHZ**: `vuln-authz.txt` (수평/수직 권한 상승 및 워크플로우 우회 정밀 분석)

---

## 🔍 단계별 상세 점검 결과

### 1단계: Pre-Recon (`pre-recon-code.txt`)
- **분석 지침**: `authz checks, RBAC/ABAC, tenant isolation`을 보안 패턴 헌팅의 핵심으로 지정 (108행).
- **역할**: 앱 전체의 권한 관리 철학(중앙 집중형 middleware vs 분산형 logic)을 파악.

### 2단계: Recon (`recon.txt`)
- **인벤토리**: `Authorization & Ownership Architecture Mapper`를 통해 모든 ID 파라미터 수집 (178행).
- **실전 타겟**: 권한 체계의 허점을 공략할 수 있는 "IDOR Candidates" 목록 생성.

### 3단계: Recon-Verify (`recon-verify.txt`)
- **지배력 분석**: 권한 가드가 모든 코드 경로를 지배(Dominate)하는지, 싱크 이전에 실행되는지 전수 검증.
- **증거**: `findings/recon-verify/authz.md`에 테넌트 격리 위반 가능성이 있는 지점 기록.

### 4단계: API Fuzzer (`api-fuzzer.txt`)
- **권한 경계 테스트**: A 사용자의 토큰으로 B 사용자의 데이터를 요청하여 서버의 거부 여부 확인.
- **안정성**: `BASH-PRECISION` 규칙으로 복잡한 객체 ID가 포함된 요청도 안전하게 전송.

### 5단계: Vuln-AUTHZ (`vuln-authz.txt`) -- **최근 대폭 강화됨**
- **권한Z 전용 분석**: 사용자 식별(AuthN)은 신뢰하고, '누가 무엇을 할 수 있는가'에만 집중하도록 설계.
- **우회 패턴 탐지**: 이중 슬래시(`//`), 대소문자 불일치 등을 이용해 권한 미들웨어를 우회하는 기법 감사 지침 추가 (Section 8.2).
- **권한 승격 감사**: 프로필 업데이트 시 `role` 필드 등을 조작하여 관리자 권한을 획득하는 **Mass Assignment** 시나리오 강화 (Section 8.2).
- **서버 사이드 식별 강제**: 인가 결정 시 사용하는 사용자/테넌트 ID는 반드시 서버 세션/토큰에서 유도되어야 하며, 클라이언트 인풋에 의존하면 즉시 취약점으로 분류 (Section 8.4).
- **워크플로우 분석**: 결제나 승인 단계에서 이전 상태를 검증하지 않고 단계를 건너뛰는(Step Skipping) 로직 정밀 분석.

---

## ✅ 점검 결론
Authorization 분석 체인은 **"권한 관리의 논리적 틈새"**를 찾아내는 데 최적화됨. 특히 단순한 IDOR를 넘어 미들웨어 우회 기법이나 권한 필드 주입 등 공격자의 창의적인 권한 상승 시나리오를 체계적으로 검증할 수 있는 '전술적 직관'을 확보함.

---
*Prepared by Antigravity (Advanced Agentic Coding)*
